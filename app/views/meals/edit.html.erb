<div id="container_p">
<h1>Edit Meal Page</h1>

<%= form_with(model: @meal, url: meal_path(@meal), local: true) do |form| %>
  <% if @meal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meal.errors.count, "error") %> prohibited this meal from being saved:</h2>

      <ul>
      <% @meal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :task_title %>
  </div>
  
  
    <div class="actions">
      <%= form.submit "Update Meal", id: "update-task" %>
    </div>
  <% end %>
<%= link_to 'Back', meals_path, id: "back" %>
</div>