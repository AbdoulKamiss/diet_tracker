<div id="container_p">
<% if flash[:notice] %>
  <div class="notice"><%= flash[:notice] %></div>
<% end %>

<h1>Show Workout Page</h1>

<h1><%= @workout.name %></h1>
<p><%= @workout.duration %> min</p>
<p><%= @workout.date %></p>

<%= link_to 'Edit', edit_workout_path(@workout), id: "edit-task" %> |
<%= link_to 'Back', workouts_path, id: "back" %>
</div>